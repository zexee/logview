diff --git a/gl/pdcdisp.c b/gl/pdcdisp.c
index ad383a6..509bd46 100644
--- a/gl/pdcdisp.c
+++ b/gl/pdcdisp.c
@@ -903,7 +903,13 @@ void PDC_doupdate(void)
         glBindFramebuffer(GL_FRAMEBUFFER, 0);
     }
 
-    SDL_GL_SwapWindow(pdc_window);
+    /* lv patch: skip the swap when the host wants to draw on top of the back
+     * buffer (e.g. Dear ImGui overlays). The host is responsible for calling
+     * SDL_GL_SwapWindow itself in that case. */
+    if (!pdc_no_swap)
+    {
+        SDL_GL_SwapWindow(pdc_window);
+    }
 
     shrink_glyph_grid();
 }
diff --git a/gl/pdcgl.h b/gl/pdcgl.h
index 4826272..3a39f7e 100644
--- a/gl/pdcgl.h
+++ b/gl/pdcgl.h
@@ -30,8 +30,13 @@ PDCEX  int pdc_font_size;
 PDCEX  int pdc_resize_mode;        /* PDC_GL_RESIZE_NORMAL by default */
 PDCEX  int pdc_interpolation_mode; /* PDC_GL_INTERPOLATE_BILINEAR by default */
 PDCEX  SDL_Window *pdc_window;
+PDCEX  SDL_GLContext pdc_gl_context;  /* lv: host can pre-set to share its context */
 PDCEX  SDL_Surface *pdc_icon;
 PDCEX  int pdc_sheight, pdc_swidth;
+/* When non-zero, PDC_doupdate() leaves the final SDL_GL_SwapWindow call to
+ * the caller so another OpenGL renderer (e.g. Dear ImGui) can draw on top of
+ * the same back buffer before the swap. */
+PDCEX  int pdc_no_swap;
 
 extern Uint32 *pdc_glyph_cache[4];
 extern int pdc_glyph_cache_size[4];
diff --git a/gl/pdcscrn.c b/gl/pdcscrn.c
index ef222f2..03efb2d 100644
--- a/gl/pdcscrn.c
+++ b/gl/pdcscrn.c
@@ -37,6 +37,7 @@ int pdc_glyph_cache_w = 0, pdc_glyph_cache_h = 0;
 int* pdc_glyph_start_col = NULL;
 
 SDL_Window *pdc_window = NULL;
+int pdc_no_swap = 0;
 SDL_Surface *pdc_icon = NULL;
 int pdc_sheight = 0, pdc_swidth = 0;
 
@@ -47,7 +48,7 @@ unsigned pdc_font_texture = 0, pdc_render_target_texture = 0;
 unsigned pdc_tex_fbo = 0;
 static GLuint pdc_vao = 0;
 
-static SDL_GLContext pdc_gl_context = NULL;
+SDL_GLContext pdc_gl_context = NULL;
 
 /* The same vertex shader is used in both shader programs, the one that draws
  * the background colors and the one that draws the foreground glyphs. Vertex
@@ -198,19 +199,25 @@ static void _clean(void)
         pdc_color_buffer = pdc_glyph_buffer = pdc_background_shader_program =
         pdc_foreground_shader_program = 0;
 
-    if(pdc_gl_context)
+    /* lv patch: when pdc_no_swap is set, the host owns the GL context, the
+     * SDL window, and the SDL lifecycle. Skip tearing them down here so the
+     * host can clean up after we return. */
+    if (!pdc_no_swap)
     {
-        SDL_GL_DeleteContext(pdc_gl_context);
-        pdc_gl_context = NULL;
-    }
+        if(pdc_gl_context)
+        {
+            SDL_GL_DeleteContext(pdc_gl_context);
+            pdc_gl_context = NULL;
+        }
 
-    if(pdc_window)
-    {
-        SDL_DestroyWindow(pdc_window);
-        pdc_window = NULL;
-    }
+        if(pdc_window)
+        {
+            SDL_DestroyWindow(pdc_window);
+            pdc_window = NULL;
+        }
 
-    SDL_Quit();
+        SDL_Quit();
+    }
     pdc_sheight = pdc_swidth = 0;
 }
 
@@ -405,30 +412,39 @@ int PDC_scr_open(void)
     SDL_GL_SetAttribute(SDL_GL_DEPTH_SIZE, 0);
     SDL_GL_SetAttribute(SDL_GL_STENCIL_SIZE, 0);
 
-    pdc_window = SDL_CreateWindow("PDCurses",
-        SDL_WINDOWPOS_CENTERED_DISPLAY(displaynum),
-        SDL_WINDOWPOS_CENTERED_DISPLAY(displaynum),
-        pdc_swidth, pdc_sheight,
-        SDL_WINDOW_RESIZABLE|SDL_WINDOW_OPENGL);
-
+    /* lv patch: if the host already set pdc_window (and optionally
+     * pdc_gl_context), attach to them instead of creating our own window.
+     * This lets an outer SDL app embed the TUI in its window. */
     if (pdc_window == NULL)
     {
-        fprintf(stderr, "Could not open SDL window: %s\n", SDL_GetError());
-        return ERR;
-    }
+        pdc_window = SDL_CreateWindow("PDCurses",
+            SDL_WINDOWPOS_CENTERED_DISPLAY(displaynum),
+            SDL_WINDOWPOS_CENTERED_DISPLAY(displaynum),
+            pdc_swidth, pdc_sheight,
+            SDL_WINDOW_RESIZABLE|SDL_WINDOW_OPENGL);
 
-    SDL_SetWindowIcon(pdc_window, pdc_icon);
+        if (pdc_window == NULL)
+        {
+            fprintf(stderr, "Could not open SDL window: %s\n", SDL_GetError());
+            return ERR;
+        }
+
+        SDL_SetWindowIcon(pdc_window, pdc_icon);
+    }
 
     SDL_GetWindowSize(pdc_window, &w, &h);
 
     if (!pdc_sheight) pdc_sheight = h;
     if (!pdc_swidth) pdc_swidth = w;
 
-    pdc_gl_context = SDL_GL_CreateContext(pdc_window);
     if (pdc_gl_context == NULL)
     {
-        fprintf(stderr, "Could not open OpenGL context: %s\n", SDL_GetError());
-        return ERR;
+        pdc_gl_context = SDL_GL_CreateContext(pdc_window);
+        if (pdc_gl_context == NULL)
+        {
+            fprintf(stderr, "Could not open OpenGL context: %s\n", SDL_GetError());
+            return ERR;
+        }
     }
     SDL_GL_MakeCurrent(pdc_window, pdc_gl_context);
 
